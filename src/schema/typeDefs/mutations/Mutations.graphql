type Mutation {
    authLogin(code: String!): AuthResponse!
    changeLang(input: inputLang!): User
    changePrefix(input: inputPrefix!): Guild
    usages(id: String!): Guild
    receiveCommands(input: [inputCommands]): [Commands]
}